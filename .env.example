# Example environment configuration for TestLog Inspector
# Copy to .env and adjust values if needed
# See ENVIRONMENT.md for documentation.

# Base URL for the NestJS API used by the Next.js frontend
NEXT_PUBLIC_API_URL=http://localhost:3001

# Port on which the NestJS API listens
API_PORT=3001

# Allowed origin for CORS requests to the API
CORS_ORIGIN=http://localhost:3000

# Maximum upload size in bytes (50MB default)
MAX_UPLOAD_SIZE=52428800

# Alternate way to set the maximum upload size in megabytes
UPLOAD_LIMIT_MB=50

# When defined, Playwright will not reuse an existing dev server
#CI=true
