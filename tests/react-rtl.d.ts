/**
 * Types globaux React Testing Library + jest-dom
 * ----------------------------------------------
 * Ce fichier est référencé via la propriété "types" de tsconfig / Vitest
 * afin que l’autocomplétion VS Code propose les matchers
 * `toBeInTheDocument`, `toHaveTextContent`, etc.
 */

import '@testing-library/jest-dom';

export {};
